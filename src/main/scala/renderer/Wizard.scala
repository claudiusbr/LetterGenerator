package renderer

import scala.swing._

class Wizard extends MainFrame {
  title = "Letter Maker Wizard" 
  preferredSize = new Dimension(320,240)
  contents = new Label("This is where the options will go")
}