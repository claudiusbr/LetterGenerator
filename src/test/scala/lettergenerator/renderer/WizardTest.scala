package lettergenerator
package renderer

import scala.swing.FileChooser
import java.io.File

import org.mockito.Mockito.when
import org.mockito.Mockito

class WizardTest extends Tester {
  describe("the Wizard class") {
    it("displays a gui") {
      assert(false)  
    }
  }
}
